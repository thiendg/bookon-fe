# React + Vite + Axios

# Guide to run this front-end
To run this front-end repo, you should install related node modules before starting build the project. You can start with:
```
npm install
npm run dev
```

# Guide to setup the project
Follow the guide to setup the project via this repo link:
https://github.com/thiendg/bookon-be